import { TPaginationParams } from "../../../shared/types/pagination";

export const defaultPaginationParams: TPaginationParams = {
  pages: 1,
  page: 1,
  perPage: 10,
  prevPage: null,
  nextPage: null,
  totalPages: 1,
};
